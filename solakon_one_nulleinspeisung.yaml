blueprint:
  name: Solakon ONE Nulleinspeisung (DE) - Dynamisch & SOC-Gesteuert
  description: |
    **Detaillierte Funktionsbeschreibung: Dynamische Nulleinspeisung mit SOC-Zonenlogik (V170)**

    Dieser Home Assistant Blueprint implementiert eine **dynamische Nulleinspeisung** für den Solakon ONE Wechselrichter. Er nutzt einen Proportionalregler (P-Regler) zur Feinsteuerung der AC-Ausgangsleistung und eine **dreistufige State-of-Charge (SOC)-Logik** zur effizienten Verwaltung des Batteriespeichers.

    ---
    ### 🧠 P-Regler & Steuerlogik
    Der Kern des Blueprints ist ein **Proportionalregler (P-Regler)**, der das **AC-Output-Limit** (Entladeleistung) des Wechselrichters dynamisch anpasst.
    * **Messprinzip:** Die Regelung basiert auf dem **Shelly/Netzleistungssensor**. **WICHTIG:** Positive Werte = Bezug, Negative Werte = Einspeisung.
    * **Korrektur-Aggressivität:** Gesteuert über den **Regelungs-Faktor**.
    * **Leistungsbegrenzung:** Gesichert durch ein **oberes Hard Limit** und ein **hartkodiertes unteres Limit von 0 W**.

    ---
    ### 🔋 Dreistufige SOC-Zonenlogik
    Die Logik unterteilt den Batterieladestand (SOC) in drei Zonen:

    #### 1. Zone: Aggressive Regelung (Entladezyklus / Hoher SOC)
    * **Bedingung:** SOC > **Oberer Schwellenwert** (`soc_fast_limit`) **ODER** Entladezyklus aktiv.
    * **Aktion:** **Max. Entladestrom** wird auf den Standardwert (z.B. 40 A) gesetzt.
    * **Regelung:** P-Regler mit **Nullpunkt-Offset von 0 W** zur exakten Nulleinspeisung.

    #### 2. Zone: Batterieschonende Regelung (Zwischen-SOC)
    * **Bedingung:** SOC zwischen Unterer (`soc_conservation_limit`) und Oberer Schwelle, UND Entladezyklus inaktiv.
    * **Aktion:** **Max. Entladestrom** wird auf **0 A** gesetzt (Entnahme nur über AC-Limit).
    * **Regelung:** P-Regler mit **Negativen Nullpunkt-Offset** (z.B. -30 W) zur Verschiebung des Regelziels hin zu leichtem **Netzbezug** (Laden).
    * **Dynamische Obergrenze:** Entladeleistung wird durch **Max(0, PV - Reserve)** gedeckelt.

    #### 3. Zone: Sicherheits-Stopp (Niedriger SOC)
    * **Bedingung:** SOC fällt auf oder unter den **Unteren Schwellenwert**.
    * **Aktion:** Sofortiges Umschalten auf **"Disabled"** und Setzen des **AC-Output-Limits auf 0 W**.

    ---
    ### ⚙️ Zusätzliche Steuermechanismen
    * **Zwei-Stufen-Moduswechsel:** Eine Puls-Sequenz (1s Puls, dann 3599s) wird verwendet, um die Modus-Kommandos zu sichern.
    * **Timeout-Refresh:** Der Remote Timeout wird automatisch auf 3599s zurückgesetzt, wenn der Countdown unter 120s fällt.
    
  domain: automation
  source_url: https://github.com/D4nte85/Solakon-One-Nulleinspeisung-Blueprint-homeassistant/blob/experimental_battery_discharge_limit/solakon_one_nulleinspeisung.yaml
  input:
    # -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
    # 🔌 EINSTELLUNGEN: Entitäten (Sortiert nach Leistung, Entlade-Limit, Modus/Select, Timer)
    # -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
    
    # POWER SENSOREN
    grid_power_sensor:
      name: Shelly/Netz-Leistungssensor ⚡
      description: |
        Sensor für die aktuelle Netzleistung (z.B. Shelly 3EM).
        POSITIVE WERTE = Bezug, NEGATIVE WERTE = Einspeisung.
      selector:
        entity:
          domain: sensor
          unit_of_measurement: W

    solar_power_sensor:
      name: Solakon ONE - Solarleistung (PV-Erzeugung) ☀️
      description: Die aktuelle PV-Gesamterzeugung in Watt.
      default: sensor.solakon_one_total_pv_power
      selector:
        entity:
          domain: sensor
          unit_of_measurement: W

    active_power_number:
      name: Solakon ONE - Ausgangsleistungsregler ⚙️
      description: Die Entität zum Setzen des Leistungs-Sollwerts (Remote Active Power).
      default: number.solakon_one_remote_active_power
      selector:
        entity:
          domain: number

    # ENTLADE LIMIT
    max_discharge_current_number:
      name: Solakon ONE - Max. Entladestrom (A) 🔋
      description: Die Entität zur Steuerung des maximalen Entladestroms (A).
      default: number.solakon_one_battery_max_discharge_current
      selector:
        entity:
          domain: number

    # MODUS/SELECT
    mode_select:
      name: Solakon ONE - Betriebsmodus-Auswahl 🔄
      description: Die Entität zum Umschalten des Betriebsmodus (Remote Control Mode).
      default: select.solakon_one_remote_control_mode
      selector:
        entity:
          domain: select
          
    # TIME SENSOREN
    remote_timeout_set_number:
      name: Modus-Reset-Timer-Entität (Setter) ⏲️
      description: Dient zum Setzen/Zurücksetzen des Remote-Timeouts (max. 3599 s).
      default: number.solakon_one_remote_timeout_set
      selector:
        entity:
          domain: number

    remote_timeout_countdown_sensor:
      name: Remote Timeout Countdown Sensor (Ausleser) ⏱️
      description: Sensor, der den verbleibenden Timeout-Countdown anzeigt.
      default: sensor.solakon_one_remote_timeout_countdown
      selector:
        entity:
          domain: sensor
          unit_of_measurement: s

    # -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
    # 📊 EINSTELLUNGEN: SOC & ZUSTAND
    # -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

    soc_sensor:
      name: Solakon ONE - Batterieladestand (SOC) 📊
      description: Der aktuelle Batterieladestand in %.
      default: sensor.solakon_one_battery_soc
      selector:
        entity:
          domain: sensor
          unit_of_measurement: "%"
          
    cycle_active_input_select:
      name: Entladezyklus-Zustandsspeicher (Helfer: Auswahl) 💾
      description: |
        WICHTIG: Erstellen Sie einen Input Select Helfer mit den Optionen 'on' und 'off'.
        Dieser speichert, ob der Entladezyklus aktiv ist (Zone 1).
      default: input_select.soc_entladezyklus_status
      selector:
        entity:
          domain: input_select

    # -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
    # 🎚️ EINSTELLUNGEN: Grenzwerte und Faktoren
    # -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

    soc_fast_limit:
      name: SOC-Schwelle "Schnelle Regelung" (Zone 1 Start)
      description: Obere Schwelle. Überschreiten startet den aggressiven Entladezyklus (z.B. 50 %).
      default: 50
      selector:
        number:
          min: 1
          max: 99
          unit_of_measurement: "%"

    soc_conservation_limit:
      name: SOC-Schwelle "Lade-Priorität" (Zone 2 Start/Zone 3 Stopp)
      description: Untere Schwelle. Unterschreiten stoppt die Entladung (Zone 3). (z.B. 20 %).
      default: 20
      selector:
        number:
          min: 1
          max: 99
          unit_of_measurement: "%"
          
    tolerance:
      name: Toleranzbereich (Halbbreite)
      description: Der zulässige Bereich in Watt um den Nullpunkt, bevor eine Korrektur vorgenommen wird (z.B. 25 W).
      default: 25
      selector:
        number:
          min: 0
          max: 200
          unit_of_measurement: W
          
    regulator_factor:
      name: Regelungs-Faktor (Aggressivität des P-Reglers)
      description: Definiert die Aggressivität, mit der die Ausgangsleistung angepasst wird (z.B. 1.5).
      default: 1.5
      selector:
        number:
          min: 0.1
          max: 5
          step: 0.1
          
    offset:
      name: Nullpunkt-Offset (Zielwert in Zone 2)
      description: Der Zielwert für die Netzleistung in Zone 2. Negativer Wert erzwingt leichten Netzbezug (z.B. -30 W).
      default: -30
      selector:
        number:
          min: -200
          max: 0
          unit_of_measurement: W

    default_discharge_current:
      name: Max. Entladestrom (Standardwert in Zone 1)
      description: Der maximale Entladestrom (A), der in der aggressiven Zone 1 gesetzt wird (z.B. 40 A).
      default: 40
      selector:
        number:
          min: 0
          max: 50
          unit_of_measurement: A

    pv_charge_reserve:
      name: 🔋 PV-Ladereserve (Watt)
      description: Die PV-Leistung, die in Zone 2 reserviert wird, um die Ladung zu gewährleisten. Dient zur dynamischen Begrenzung der Ausgangsleistung (z.B. 50 W).
      default: 50
      selector:
        number:
          min: 0
          max: 1000
          unit_of_measurement: W
          
    max_active_power_limit:
      name: Maximale Ausgangsleistung (Hard Limit)
      description: Die maximale AC-Ausgangsleistung, die das Blueprint setzen darf (z.B. 800 W).
      default: 800
      selector:
        number:
          min: 0
          max: 1000
          unit_of_measurement: W

mode: queued
max_exceeded: silent
variables:
  # Zonen-Schwellwerte
  soc_fast_limit_int: !input soc_fast_limit
  soc_conservation_limit_int: !input soc_conservation_limit
  
  # Aktuelle Sensorwerte
  grid_power: !input grid_power_sensor
  soc: !input soc_sensor
  solar_power: !input solar_power_sensor
  timeout_countdown: !input remote_timeout_countdown_sensor
  
  # Konfiguration
  tolerance_int: !input tolerance
  regulator_factor_float: !input regulator_factor
  offset_int: !input offset
  max_active_power_limit_int: !input max_active_power_limit
  pv_charge_reserve_int: !input pv_charge_reserve
  
  # Zustände
  cycle_active: !input cycle_active_input_select
  solakon_mode: !input mode_select
  
  # Parameter
  discharge_current_max: !input default_discharge_current
  
  # Entitäten
  active_power_entity: !input active_power_number
  discharge_current_entity: !input max_discharge_current_number
  timeout_set_entity: !input remote_timeout_set_number


trigger:
  # -------------------------------------------------------------------------------------------------
  # 1. Leistungsänderungen (Verzögerte Trigger für stabile P-Regelung)
  # -------------------------------------------------------------------------------------------------
  - platform: state
    entity_id: !input grid_power_sensor
    for:
      seconds: 3
  - platform: state
    entity_id: !input solar_power_sensor
    for:
      seconds: 3

  # -------------------------------------------------------------------------------------------------
  # 2. SOC-Schwellwert-Erreichung (Steuert Zonen-Wechsel)
  # -------------------------------------------------------------------------------------------------
  - platform: numeric_state
    entity_id: !input soc_sensor
    above: !input soc_fast_limit

  - platform: numeric_state
    entity_id: !input soc_sensor
    below: !input soc_conservation_limit

  # -------------------------------------------------------------------------------------------------
  # 3. Modus-Wechsel (Reagiert auf manuelle/externe Änderung)
  # -------------------------------------------------------------------------------------------------
  - platform: state
    entity_id: !input mode_select

action:
  # -------------------------------------------------------------------------------------------------
  # VALIDIERUNG: Check auf kritische Fehler (Werte und Entitäten)
  # -------------------------------------------------------------------------------------------------
  - choose:
      # PRÜFUNG A: SOC-Limits
      - conditions:
          - condition: template
            value_template: "{{ soc_fast_limit_int <= soc_conservation_limit_int }}"
        sequence:
          - service: system_log.write
            data:
              level: error
              message: "Solakon ONE Blueprint Fehler: Die obere SOC-Schwelle ({{ soc_fast_limit_int }}%) muss größer sein als die untere SOC-Schwelle ({{ soc_conservation_limit_int }}%). Automatisierung gestoppt."
          - stop: Die SOC-Limits sind falsch gesetzt.
          
      # PRÜFUNG B: Kritische Entitäten verfügbar
      - conditions:
          - condition: or
            conditions:
              - condition: template
                value_template: "{{ states(soc) is not number or states(timeout_countdown) is not number }}"
              - condition: template
                value_template: "{{ states(grid_power) | is_number is false or states(grid_power) == 'unknown' or states(grid_power) == 'unavailable' }}"
              - condition: template
                value_template: "{{ states(solakon_mode) | is_state('unknown') or states(solakon_mode) | is_state('unavailable') }}"
              - condition: template
                value_template: "{{ state_attr(active_power_entity, 'min') is none }}"
        sequence:
          - service: system_log.write
            data:
              level: error
              message: "Solakon ONE Blueprint Fehler: Eine oder mehrere kritische Entitäten (SOC, Timeout-Sensor, Netzleistung, Ausgangsleistungsregler) sind UNVERFÜGBAR oder haben ungültige Werte. Automatisierung gestoppt."
          - stop: Kritische Entitäten sind fehlerhaft.
          
    default:
      # -------------------------------------------------------------------------------------------------
      # HAUPT-LOGIK: Modus- und Zyklus-Steuerung (Zone 1, 2, 3)
      # -------------------------------------------------------------------------------------------------
      - choose:
          # FALL A: ZONE 1 - Aggressive Entladung START (SOC > Obere Schwelle UND Zyklus OFF)
          - conditions:
              - condition: template
                value_template: "{{ states(soc) | float(0) > soc_fast_limit_int }}"
              - condition: state
                entity_id: !input cycle_active_input_select
                state: 'off'
            sequence:
              # 1. Zyklus auf 'on' setzen
              - service: input_select.select_option
                data:
                  entity_id: !input cycle_active_input_select
                  option: 'on'
              # 2. Max. Entladestrom setzen (für Zone 1)
              - service: number.set_value
                data:
                  entity_id: !input max_discharge_current_number
                  value: "{{ discharge_current_max }}"
              # 3. Modus-Wechsel zu 'INV Discharge PV Priority' (mit Puls-Sequenz zur Sicherheit)
              - choose:
                  - conditions: "{{ states(solakon_mode) != 'INV Discharge (PV Priority)' }}"
                    sequence:
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 1
                      - delay: 1s
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 3599
              - service: select.select_option
                data:
                  entity_id: !input mode_select
                  option: 'INV Discharge (PV Priority)'

          # FALL B: ZONE 3 - Sicherheits-STOPP (SOC <= Untere Schwelle UND Zyklus ON)
          - conditions:
              - condition: template
                value_template: "{{ states(soc) | float(0) <= soc_conservation_limit_int }}"
              - condition: state
                entity_id: !input cycle_active_input_select
                state: 'on'
            sequence:
              # 1. Zyklus auf 'off' setzen
              - service: input_select.select_option
                data:
                  entity_id: !input cycle_active_input_select
                  option: 'off'
              # 2. Modus-Wechsel zu 'Disabled' (mit Puls-Sequenz zur Sicherheit)
              - choose:
                  - conditions: "{{ states(solakon_mode) != 'Disabled' }}"
                    sequence:
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 1
                      - delay: 1s
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 3599
              - service: select.select_option
                data:
                  entity_id: !input mode_select
                  option: 'Disabled'
              # 3. Power Limit auf 0 W setzen (Soft Stopp)
              - service: number.set_value
                data:
                  entity_id: !input active_power_number
                  value: 0
                  
          # FALL C: ZONE 3 - Sicherheits-STOPP (Zusätzliche Absicherung, falls Zyklus bereits OFF aber Modus aktiv)
          - conditions:
              - condition: template
                value_template: "{{ states(soc) | float(0) < soc_conservation_limit_int }}"
              - condition: state
                entity_id: !input cycle_active_input_select
                state: 'off'
              - condition: template
                value_template: "{{ states(solakon_mode) != 'Disabled' }}"
            sequence:
              # Modus auf 'Disabled' setzen
              - choose:
                  - conditions: "{{ states(solakon_mode) != 'Disabled' }}"
                    sequence:
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 1
                      - delay: 1s
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 3599
              - service: select.select_option
                data:
                  entity_id: !input mode_select
                  option: 'Disabled'
              # Power Limit auf 0 W setzen
              - service: number.set_value
                data:
                  entity_id: !input active_power_number
                  value: 0
                  
          # FALL D: ZONE 2 - Batterieschonend START (P-Regler aktiv, aber 0 A Entladung)
          - conditions:
              - condition: template
                value_template: "{{ states(soc) | float(0) > soc_conservation_limit_int and states(soc) | float(0) <= soc_fast_limit_int }}"
              - condition: state
                entity_id: !input cycle_active_input_select
                state: 'off'
              - condition: template
                value_template: "{{ states(solakon_mode) != 'INV Discharge (PV Priority)' }}"
            sequence:
              # 1. Max. Entladestrom auf 0 A setzen (Batterieschonung)
              - service: number.set_value
                data:
                  entity_id: !input max_discharge_current_number
                  value: 0
              # 2. Modus-Wechsel zu 'INV Discharge PV Priority'
              - choose:
                  - conditions: "{{ states(solakon_mode) != 'INV Discharge (PV Priority)' }}"
                    sequence:
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 1
                      - delay: 1s
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 3599
              - service: select.select_option
                data:
                  entity_id: !input mode_select
                  option: 'INV Discharge (PV Priority)'
          
      # -------------------------------------------------------------------------------------------------
      # AKTION: P-Regler Logik und Timeout-Refresh
      # -------------------------------------------------------------------------------------------------
      
      - choose:
          # Bedingung: Nur ausführen, wenn der Modus aktiv ist (Zone 1 oder 2)
          - conditions:
              - condition: template
                value_template: "{{ states(solakon_mode) == 'INV Discharge (PV Priority)' }}"
            sequence:
              # 1. Timeout-Reset (Refresh) bei kritischem Wert (< 120s)
              - choose:
                  - conditions:
                      - condition: template
                        value_template: "{{ states(timeout_countdown) | int(9999) < 120 }}"
                    sequence:
                      - service: number.set_value
                        data:
                          entity_id: !input remote_timeout_set_number
                          value: 3599
                      - delay: 50ms # Kurze Verzögerung, um den Reset zu verarbeiten
                          
              # 2. P-Regler Logik (Regelung des Power Limits)
              - variables:
                  # Netzleistung als Float
                  grid_power_float: "{{ states(grid_power) | float(0) }}"
                  # Aktuelle Ausgangsleistung für die Korrektur
                  current_active_power: "{{ states(active_power_entity) | float(0) }}"
                  # Berechneter Nullpunkt-Offset (0 W in Zone 1, -Offset in Zone 2)
                  target_offset: >
                    {% if is_state(cycle_active, 'on') %}
                      0
                    {% else %}
                      {{ offset_int }}
                    {% endif %}
                  # Berechnete Korrekturleistung (P-Regler)
                  power_correction: "{{ (grid_power_float - target_offset) * regulator_factor_float }}"
                  # Die neu berechnete Solleistung (unbegrenzt)
                  new_power_unlimited: "{{ current_active_power + power_correction }}"
                  
              - choose:
                  # Bedingung: Außerhalb des Toleranzfensters?
                  - conditions:
                      - condition: template
                        value_template: "{{ (grid_power_float - target_offset) | abs > tolerance_int }}"
                    sequence:
                      - choose:
                          # FALL B1: ZONE 2 - Batterieschonend (Zyklus OFF) -> Gedeckelt durch PV-Reserve
                          - conditions:
                              - condition: state
                                entity_id: !input cycle_active_input_select
                                state: 'off'
                            sequence:
                              - variables:
                                  # Max Power Limit in Zone 2 = Max(0, PV - Reserve)
                                  max_zone_2_limit: >
                                    {% set pv_minus_reserve = states(solar_power) | float(0) - pv_charge_reserve_int %}
                                    {{ [0, pv_minus_reserve] | max }}
                                  # Finale Power (min von P-Regler und Zone 2 Limit)
                                  final_power: "{{ [max_zone_2_limit, new_power_unlimited] | min }}"
                              - service: number.set_value
                                data:
                                  entity_id: !input active_power_number
                                  value: "{{ [0, final_power] | max | round(0) }}" # Min 0 W
                                  
                          # FALL B2: ZONE 1 - Aggressiv (Zyklus ON) -> Gedeckelt durch Hard Limit
                          - conditions:
                              - condition: state
                                entity_id: !input cycle_active_input_select
                                state: 'on'
                            sequence:
                              - variables:
                                  # Finale Power (min von P-Regler und Hard Limit)
                                  final_power: "{{ [max_active_power_limit_int, new_power_unlimited] | min }}"
                              - service: number.set_value
                                data:
                                  entity_id: !input active_power_number
                                  value: "{{ [0, final_power] | max | round(0) }}" # Min 0 W
